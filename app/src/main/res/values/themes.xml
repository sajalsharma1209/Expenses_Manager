<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ExpensesManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/yellow</item>
    </style>

    <style name="Theme.ExpensesManager" parent="Base.Theme.ExpensesManager" />

    <style name="App.CustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/yellowDark</item>
    </style>
</resources>